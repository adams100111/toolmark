[
  {
    "page": "page-a",
    "tools": [
      {
        "name": "search.run",
        "llmName": "search_run",
        "description": "Searches the catalog for products matching a free-text query.",
        "hints": { "readOnly": true },
        "inputSchema": {
          "type": "object",
          "properties": {
            "query": { "type": "string", "description": "Free-text search query." }
          }
        }
      }
    ]
  },
  {
    "page": "page-b",
    "tools": [
      {
        "name": "cart.add",
        "llmName": "cart_add",
        "description": "Adds one item to the shopping cart by its product id.",
        "hints": {},
        "inputSchema": {
          "type": "object",
          "properties": {
            "productId": { "type": "string", "description": "Id of the product to add." }
          }
        }
      }
    ]
  }
]
