name: PR workflow env secrets

on:
  pull_request:

permissions: {}

concurrency:
  group: pr-${{ github.ref }}
  cancel-in-progress: true

env:
  TOKEN: ${{ secrets.DEPLOY_TOKEN }}

jobs:
  test:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - run: echo ok
